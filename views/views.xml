<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="extend_lead.list">
      <field name="name">extend_lead list</field>
      <field name="model">extend_lead.extend_lead</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="extend_lead.action_window">
      <field name="name">extend_lead window</field>
      <field name="res_model">extend_lead.extend_lead</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="extend_lead.action_server">
      <field name="name">extend_lead server</field>
      <field name="model_id" ref="model_extend_lead_extend_lead"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="extend_lead" id="extend_lead.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="extend_lead.menu_1" parent="extend_lead.menu_root"/>
    <menuitem name="Menu 2" id="extend_lead.menu_2" parent="extend_lead.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="extend_lead.menu_1_list" parent="extend_lead.menu_1"
              action="extend_lead.action_window"/>
    <menuitem name="Server to list" id="extend_lead" parent="extend_lead.menu_2"
              action="extend_lead.action_server"/>
    -->
  </data>
</odoo>